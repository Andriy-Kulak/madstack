# madstack for Claude Code

This repo uses `AGENTS.md` as the canonical agent instruction file.

Before working in this repo, read and follow `AGENTS.md`. In particular: never write, print, commit, store, or log real API keys. Only document env var names such as `GEMINI_API_KEY`, `GOOGLE_API_KEY`, and `FAL_KEY`.
